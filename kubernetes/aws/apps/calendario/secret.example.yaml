apiVersion: v1
kind: Secret

metadata:
  name: calendario-secret
  namespace: ecociente

type: Opaque

stringData:
  DB_URL: "<DB_URL>"
  DB_USER: "<DB_USER>"
  DB_PASSWORD: "<DB_PASSWORD>"
  JWT_SECRET: "<JWT_SECRET>"